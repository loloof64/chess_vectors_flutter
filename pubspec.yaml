name: chess_vectors_flutter
description: Chess pieces vectors for Flutter. These vectors are custom widgets. They have their own size they can also be moved (position
             property) inside their parent, and be resize (size property).
version: 1.0.6
author: Laurent Bernabe <laurent.bernabe@gmail.com>
homepage: https://github.com/loloof64/chess_vectors_flutter

environment:
    sdk: ">=1.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tuple: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
